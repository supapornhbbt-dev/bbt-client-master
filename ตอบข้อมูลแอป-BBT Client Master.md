# ข้อมูลแอป BBT Client Master

จัดทำ 3 กันยายน 2569 · สำหรับส่งให้คุณ Meth
ข้อ 1–5 ตรวจจากไฟล์ในโปรเจกต์และฐานข้อมูลจริง · ข้อ 6–8 เป็นคำถามที่ต้องให้เจ้าของแอปตอบ

---

## 1. แอปนี้ทำอะไร ตอนนี้ถึงไหนแล้ว

เป็น **ทะเบียนลูกค้ากลางของสำนักงานบัญชี BBT พร้อมกระดานติดตามงาน** — เก็บทะเบียนลูกค้า บริการที่รับทำ ทีมผู้รับผิดชอบรายปี ชั่วโมงทำงาน และติดตามงานยื่นภาษีรายเดือน/รายปี งานจดทะเบียน งานชำระบัญชี งานตรวจสอบบัญชี

เริ่มสร้างฐานข้อมูล **19 กรกฎาคม 2569** แก้ไขล่าสุด **3 กันยายน 2569** (แก้ต่อเนื่องแทบทุกวัน)

สถานะ: **ใช้งานจริงแล้ว** ทีมงานเปิดใช้ประจำวัน มีลูกค้าในระบบ 220 ราย พนักงาน 23 รายชื่อ บัญชีเข้าใช้งาน 22 บัญชี การมอบหมายงาน 2,176 รายการ และข้อมูลติดตามภาษีรายเดือน 1,190 ช่อง — ยังเพิ่มหน้าจอใหม่อยู่เรื่อย ๆ (หน้าจอล่าสุดสร้างวันนี้)

---

## 2. สร้างด้วยอะไร

**ไม่มีไฟล์ package.json** — โปรเจกต์นี้ไม่ได้ใช้ Next.js หรือกรอบงานสำเร็จรูปใด ๆ

เป็น **หน้าเว็บไฟล์เดียวล้วน ๆ** (`webapp/index.html` ขนาดประมาณ 573 กิโลไบต์ ราว 5,900 บรรทัด) เขียนด้วยภาษาเว็บพื้นฐาน คือ HTML, CSS และ JavaScript ทั้งแอปรวมอยู่ในไฟล์นี้ไฟล์เดียว

ฐานข้อมูลใช้ **Supabase** (ฐานข้อมูล PostgreSQL แบบเช่าใช้บนคลาวด์)

เครื่องมือเสริมมีแค่ 2 ตัว ที่ดึงมาจากคลังกลางบนอินเทอร์เน็ตตอนเปิดหน้าเว็บ คือ ตัวเชื่อมต่อ Supabase และ ตัวอ่าน-เขียนไฟล์ Excel (ใช้สำหรับนำเข้า/ส่งออกข้อมูล) · นอกจากนี้มีโปรแกรมเล็ก ๆ อีก 1 ตัวฝากรันไว้บน Supabase ทำหน้าที่ให้ผู้ดูแลระบบสร้างบัญชีผู้ใช้ใหม่ได้

---

## 3. บัญชีอยู่ชื่อใคร

**GitHub — ยังไม่มี** สั่งตรวจแล้วได้ผลว่าโฟลเดอร์นี้ไม่ได้เป็นตู้เก็บโค้ดของ Git เลย แปลว่ายังไม่เคยเชื่อมกับ GitHub และ **ไม่มีประวัติการแก้ไขย้อนหลัง** มีแค่ไฟล์สำรองที่ก๊อปปี้ไว้ในเครื่องเอง

**Supabase — รหัสโปรเจกต์คือ `ljxllbafngsqlhbcwrsy`** (ส่วนที่อยู่หน้า `.supabase.co`)

**Vercel — ไม่มี** ไม่พบโฟลเดอร์ `.vercel` ในโปรเจกต์ · เว็บนี้ฝากไว้ที่ **Cloudflare Pages** แทน (พบโฟลเดอร์ตั้งค่าของเครื่องมือฝากเว็บของ Cloudflare) และเคยฝากที่ Netlify มาก่อนแต่เลิกใช้แล้ว

---

## 4. มีระบบล็อกอินของตัวเองไหม

**มีระบบล็อกอินของตัวเอง มีรหัสผ่านของแต่ละคน** ไม่ได้ต่อกับประตูล็อกอินกลางของสำนักงาน

วิธีเข้าใช้: พนักงานพิมพ์ **ตัวย่อของตัวเอง** (เช่น ตัวย่อ 3 ตัวอักษร) กับรหัสผ่าน แล้วระบบจะแปลงตัวย่อเป็นอีเมลของสำนักงานโดยอัตโนมัติ แล้วส่งไปตรวจกับระบบสมาชิกของ Supabase

**ปิดการสมัครสมาชิกเอง** — คนนอกสมัครเข้ามาไม่ได้ ต้องให้ผู้ดูแลระบบกดสร้างบัญชีให้เท่านั้น ปัจจุบันมี 22 บัญชี · มีปุ่ม "ลืมรหัสผ่าน" และ "เปลี่ยนรหัสผ่าน" ให้ใช้เองได้

**ข้อควรระวังสำคัญ:** การกำหนดสิทธิ์ในแอปนี้เป็นแค่ **การซ่อนหน้าจอ** เท่านั้น ในระดับฐานข้อมูลจริง ทุกบัญชีที่ล็อกอินได้สามารถอ่านและแก้ข้อมูลได้ทุกตารางเท่ากันหมด (ถ้ารู้วิธีเรียกผ่านช่องทางเทคนิค) — เรื่องนี้ควรแก้ก่อนเชื่อมต่อกับระบบอื่น

---

## 5. เก็บรายชื่อลูกค้าหรือพนักงานไว้เองไหม

**เก็บไว้เอง** ฐานข้อมูลมีทั้งหมด 22 ตาราง ตารางที่เกี่ยวกับรายชื่อโดยตรงคือ

**ตาราง `client` (ทะเบียนลูกค้า, 220 แถว)** ชื่อช่องข้อมูล:
`tax_id` (เลขประจำตัวผู้เสียภาษี ใช้เป็นกุญแจหลัก), `client_code` (ตัวย่อบริษัท), `name_th` (ชื่อบริษัท), `dbd_type`, `nature_id`, `client_group`, `status`, `record_mode`, `vat_registered`, `vat_reg_date_be`, `registered_capital`, `income_tax_scheme`, `fiscal_year_end`, `fiscal_group`, `software`, `software_db`, `note`, `client_type`, `updated_at`

**ตาราง `staff` (ทะเบียนพนักงาน, 23 แถว)** ชื่อช่องข้อมูล:
`staff_token` (ตัวย่อพนักงาน), `employee_id`, `token_en`, `name_th`, `name_en`, `nickname`, `level` (ระดับตำแหน่ง), `start_date`, `status`, `resign_date`, `updated_at`

ตารางอื่นที่เกี่ยวข้อง: `assignment` (ใครทำบริษัทไหน บทบาทอะไร ปีไหน), `service_job` (บริการที่แต่ละบริษัทใช้), `app_user` (บทบาทของบัญชีผู้ใช้), และตารางฝั่งงานตรวจสอบบัญชีอีกชุดหนึ่ง

**ที่มาของรายชื่อ:** ตั้งต้นด้วยการ **นำเข้าจากไฟล์ Excel ของสำนักงาน** เมื่อ 19 ก.ค. 2569 · เสริมด้วยข้อมูลที่ดึงจากเว็บทะเบียนธุรกิจของกรมพัฒนาธุรกิจการค้า (เช่น ประเภทธุรกิจ วันจดทะเบียนเลิก) · หลังจากนั้นทีมงาน **แก้ไขและเพิ่มเองผ่านหน้าเว็บ** เป็นหลัก ไม่ได้ดึงอัตโนมัติจากระบบอื่น

---

## 6. ตอนนี้มีข้อมูลลูกค้าจริงอยู่ในแอปไหม

**คำถามถึงเจ้าของแอป:** ที่กรอกอยู่ในระบบตอนนี้ เป็นชื่อบริษัทจริง เลขผู้เสียภาษีจริง และตัวเลขจริงของลูกค้าใช่ไหม หรือเป็นข้อมูลสมมติที่คิดขึ้นมาทดลอง — ถ้าเป็นของจริง ประมาณกี่ราย

*(ข้อสังเกตจากการตรวจ: ในระบบมีลูกค้า 220 ราย พนักงาน 23 รายชื่อ และผูกที่อยู่ไฟล์เอกสารไว้ 1,469 รายการ — ดูมีลักษณะเป็นข้อมูลใช้งานจริง แต่ให้เจ้าของแอปยืนยันอีกครั้ง)*

---

## 7. ข้อมูลที่อยากได้จากแอปอื่น และข้อมูลที่แอปอื่นอยากได้จากแอปนี้

**คำถามถึงเจ้าของแอป:** เวลาทำงานจริง ต้องเปิดดูข้อมูลจากที่อื่นตรงไหนบ้าง (เช่น โปรแกรมบัญชี ระบบเบิกจ่าย ไฟล์ใน OneDrive) และงานที่ทำเสร็จในแอปนี้ ใครต้องเอาไปทำต่อบ้าง

*(ข้อสังเกตจากการตรวจ: ตอนนี้แอปเชื่อมกับ **โฟลเดอร์ OneDrive ของสำนักงาน** อยู่แล้วในลักษณะอ่านชื่อไฟล์ — เก็บเป็นที่อยู่อ้างอิงไว้ 1,390 รายการ ไม่ได้ก๊อปตัวไฟล์เข้ามา · และมีการนำเข้า-ส่งออกไฟล์ Excel กับข้อมูลจากเว็บกรมพัฒนาธุรกิจการค้า)*

---

## 8. ข้อมูลกับไฟล์ในแอปนี้ใหญ่แค่ไหน

**คำถามถึงเจ้าของแอป:** รบกวนเปิดหน้าเว็บ Supabase → เมนู Settings → Usage แล้วแจ้งตัวเลข 2 ตัวคือ **Database size** และ **Storage size** · และดูที่หน้าแรกของ Supabase ว่าตอนนี้มีโปรเจกต์อยู่กี่อัน (แบบใช้ฟรีมีได้ 2 อัน)

*(ข้อสังเกตจากการตรวจ: ปริมาณข้อมูลน่าจะยังไม่มาก ตารางที่ใหญ่ที่สุดมีราวสองพันแถว · และ **ไฟล์เอกสารส่วนใหญ่ไม่ได้เก็บบน Supabase** ระบบเก็บแค่ "ที่อยู่ไฟล์" ที่ชี้ไปยัง OneDrive จำนวน 1,390 รายการ จึงน่าจะใช้พื้นที่เก็บไฟล์น้อย แต่ต้องดูตัวเลขจริงจากหน้า Usage ยืนยัน)*

---

## สิ่งที่ยังไม่แน่ใจ

1. **ตัวเลขพื้นที่ใช้งานจริงบน Supabase (ข้อ 8)** — ดูจากในโค้ดไม่ได้ ต้องเปิดหน้าเว็บ Supabase ดูเอง และไม่ทราบว่าบัญชี Supabase นี้มีโปรเจกต์อื่นอยู่อีกกี่อัน
2. **บัญชี Supabase และ Cloudflare อยู่ในชื่อใคร** — ในโปรเจกต์เห็นแค่รหัสโปรเจกต์ ไม่มีข้อมูลว่าใครเป็นเจ้าของบัญชี (จากที่คุยกันก่อนหน้านี้ เข้าใจว่าผูกกับบัญชีส่วนตัวของเจ้าของแอป แต่ยังไม่ได้ยืนยัน)
3. **ข้อมูลในระบบเป็นของจริงหรือของทดลอง (ข้อ 6)** — ตรวจเองไม่ได้ตามกฎที่ห้ามเอาข้อมูลลูกค้ามาแสดง
4. **ความต้องการเชื่อมกับระบบอื่น (ข้อ 7)** — เป็นเรื่องการทำงานจริง ต้องให้เจ้าของแอปตอบ
5. **ไม่มีประวัติการแก้ไขโค้ด** — เพราะยังไม่ได้เชื่อมกับ GitHub จึงบอกได้แค่วันที่แก้ไขไฟล์ล่าสุด ไม่สามารถบอกได้ว่าใครแก้อะไรไปบ้างในแต่ละครั้ง
6. **ไม่มีบันทึกว่าใครแก้ข้อมูลอะไรในแอป** — มีตารางสำหรับเก็บประวัติอยู่ (มีข้อมูลค้างอยู่ 102 แถวจากตอนติดตั้ง) แต่ตัวแอปไม่ได้เขียนบันทึกลงตารางนี้ จึงสืบย้อนไม่ได้ว่าใครแก้ตัวเลขไหน
7. **ยังไม่ได้ทดสอบแอปบนโปรแกรมเปิดเว็บทุกยี่ห้อ** — ฟังก์ชันอ่านโฟลเดอร์ OneDrive ใช้ได้เฉพาะ Chrome กับ Edge เท่านั้น (ทั้ง Windows และ Mac) ใช้กับ Safari และ Firefox ไม่ได้
